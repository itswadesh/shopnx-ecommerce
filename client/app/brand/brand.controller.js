'use strict';

angular.module('shopnxApp')
  .controller('BrandCtrl', function ($scope, socket, Brand, Modal, toastr) {

    // var columns = ['name','info','parent','image'];

    // $scope.data = [];
    // var data = $scope.data =
    // Brand.query(function(data) {
    //   socket.syncUpdates('brand', $scope.data);
    // });

  });
